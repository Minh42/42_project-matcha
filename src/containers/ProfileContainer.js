import React from 'react';
import ReactDOM from 'react-dom';
import FormContainer from './FormContainer';

class RedirectMailContainer extends React.Component{
	constructor(props) {
		super(props);
	  }

	render () {
	return (
		<div className="column is-6 is-offset-3">
			You can Login to Matcha
		</div>
	)}
}

export default RedirectMailContainer;